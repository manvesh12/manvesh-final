spring:
  application:
    name: dsr-backend
  datasource:
    url: jdbc:postgresql://localhost:5440/dsr_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  data:
    redis:
      host: localhost
      port: 6380

app:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expirationMs: 86400000
    refreshExpirationMs: 604800000

minio:
  url: http://localhost:9010
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: dsr-pdfs

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
